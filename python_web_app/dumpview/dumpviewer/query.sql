SELECT a.in_or_out,pmn.prediction_model_id as P_id,dn.descriptor_id as D_id,concat(pmn.name," (id : ",pmn.prediction_model_id,")") as P_name,dn.descriptor_id as D_id,concat(dn.name," (id : ",dn.descriptor_id,")") as D_name,pm.creator_id as C_id,a.Dic_name,pm.description as P_description from (select pmd.descriptor_id,pmd.prediction_model_id,pmd.in_or_out,f_name2.Dic_name from prediction_model_descriptor as pmd left join	(select fpm.prediction_model_id,f_name.Dic_name	from folder_prediction_model as fpm	left join	(select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id from folder as f1 left join( select a.folder_id,a.Dic_name from (select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id from folder as f1 left join( select a.folder_id,a.Dic_name from (select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id from folder as f1 left join(select a.folder_id,a.Dic_name from (select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id from folder as f1 left join(select a.folder_id,a.Dic_name from (select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id from folder as f1 left join(select a.folder_id,a.Dic_name from (select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id from folder as f1 left join(select a.folder_id,a.Dic_name from (select f1.folder_id,case when a.Dic_name is not null then a.Dic_name else f1.folder_name end as Dic_name,case when f1.parent_folder_id = a.folder_id then null else f1.parent_folder_id end as parent_folder_id	from folder as f1 left join(select folder_id,folder_name as Dic_name from folder where parent_folder_id is null)as a on (f1.parent_folder_id = a.folder_id)) as a where a.parent_folder_id is null) as a on (a.folder_id = f1.parent_folder_id))as a where a.parent_folder_id is null) as a on (a.folder_id = f1.parent_folder_id)) as a where a.parent_folder_id is null) as a on (a.folder_id = f1.parent_folder_id)) as a where a.parent_folder_id is null) as a on (a.folder_id = f1.parent_folder_id)) as a where a.parent_folder_id is null) as a on (a.folder_id = f1.parent_folder_id)) as a where a.parent_folder_id is null) as a on (a.folder_id = f1.parent_folder_id)) as f_name	on (f_name.folder_id = fpm.folder_id)) as f_name2 on (f_name2.prediction_model_id = pmd.prediction_model_id)) as a LEFT JOIN (select name, prediction_model_id from prediction_model_name where preferred = 1 group by prediction_model_id) as pmn ON (a.prediction_model_id = pmn.prediction_model_id) LEFT JOIN (select name, descriptor_id from descriptor_name where preferred = 1 group by descriptor_id) as dn ON (a.descriptor_id = dn.descriptor_id)LEFT JOIN prediction_model as pm ON (a.prediction_model_id = pm.prediction_model_id)order by description;
